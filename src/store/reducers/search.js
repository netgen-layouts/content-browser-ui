// import { LOADING_START, LOADING_STOP } from '../actionTypes';

const INITIAL_STATE = {
  isLoading: false
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
